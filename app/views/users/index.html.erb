<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
　　　<tr><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></tr> 
  <table class="table">
   <tbody><tr></tr>
    <tr>
	    <th>name</th>
	    <th><%= @user.name %></th>
    </tr>
    <tr>
	    <th>introduction</th>
	    <th></th>
	    <th><%= @user.introduction %></th>
    </tr>
   </tbody>
  </table>
  
<div class="row">
<li><%= link_to "", edit_user_path(current_user), class: "fas fa-user-cog" %></li>
</div>

<%= render 'books/new', books: @books %>

<div class="col-md-8 offset-md-1">
  <h2>Users</h2>
  <table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th>image</th>
        <th>name</th>
      </tr>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
  <% @users.each do |user| %>  
      <tr>
        <td>
          <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
        </td> 
        <td><%= user.name %></td>
      　<td><%= link_to "show", user_path(user.id) %></td>
      </tr>
    </tbody>
  <% end %>  
  </table>
</div>